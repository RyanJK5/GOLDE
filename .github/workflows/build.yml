name: Build & Test
on:
  push:
    branches: [ "master" ]
    tags:
      - 'v*'
  pull_request:
    branches: [ "master" ]
jobs:
  build:
    name: Build
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            name: linux-gcc
          - os: windows-latest
            name: windows-msvc
          - os: macos-latest
            name: macos-clang
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Install Dependencies (Linux)
      if: runner.os == 'Linux'
      run: |
        sudo apt-get update
        sudo apt-get install -y cmake ninja-build g++-14 libx11-dev libxcursor-dev libxrandr-dev libxinerama-dev libxi-dev libxext-dev libxfixes-dev libgl1-mesa-dev libglu1-mesa-dev pkg-config libgtk-3-dev
        sudo update-alternatives --install /usr/bin/g++ g++ /usr/bin/g++-14 100
        sudo update-alternatives --install /usr/bin/gcc gcc /usr/bin/gcc-14 100
        sudo update-alternatives --install /usr/bin/c++ c++ /usr/bin/g++-14 100

    - name: Install Dependencies (Windows)
      if: runner.os == 'Windows'
      run: choco install ninja

    - name: Configure CMake (macOS)
      if: runner.os == 'macOS'
      run: |
        brew install llvm ninja
        LLVM_PATH="$(brew --prefix llvm)"
        SDK_PATH="$(xcrun --show-sdk-path)"
        cmake -B build -G Ninja \
          -DCMAKE_BUILD_TYPE=Release \
          -DCMAKE_C_COMPILER="$LLVM_PATH/bin/clang" \
          -DCMAKE_CXX_COMPILER="$LLVM_PATH/bin/clang++" \
          -DCMAKE_CXX_FLAGS="-stdlib=libc++ -isysroot $SDK_PATH" \
          -DBUILD_SHARED_LIBS=OFF
          -DCMAKE_OSX_ARCHITECTURES "x86_64;arm64"
          -DCMAKE_OSX_DEPLOYMENT_TARGET "13.3"
    
    - name: Configure CMake (Windows)
      if: runner.os == 'Windows'
      run: cmake -B build -G Ninja -DBUILD_SHARED_LIBS=OFF -DCMAKE_MSVC_RUNTIME_LIBRARY=MultiThreaded
          
    - name: Configure CMake (Linux)
      if: runner.os == 'Linux'
      run: | 
        cmake -B build -G Ninja \
          -DCMAKE_BUILD_TYPE=Release \
          -DBUILD_SHARED_LIBS=ON \
          -DCMAKE_POSITION_INDEPENDENT_CODE=ON
        

    - name: Setup MSVC Developer Command Prompt (Windows)
      if: runner.os == 'Windows'
      uses: ilammy/msvc-dev-cmd@v1


    - name: Build
      run: cmake --build build --parallel --config Release

    - name: Test
      run: ctest --test-dir build --build-config Release --output-on-failure
    
    - name: Save Build Artifacts
      uses: actions/upload-artifact@v4
      with:
        name: build-${{ matrix.name }}
        path: build/